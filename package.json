{
  "name": "cbiServerUtils",
  "version": "1.2.1",
  "main": "bundle.js",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.52.0",
    "isomorphic-fetch": "^2.2.1",
    "listr": "^0.12.0"
  },
  "scripts": {
    "prepublish": "rm -f bundle.js && npm build",
    "build": "rollup -c"
  },
  "devDependencies": {
    "rollup": "^0.41.6"
  }
}
